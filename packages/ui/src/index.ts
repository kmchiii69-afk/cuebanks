// @cuebanks/ui — formerly held shared marketing primitives and lifted
// widgets. After the deep-parity alignment with awfx, those primitives live
// in apps/web/components/shared/ and the marketing widgets live in
// apps/web/components/{page2,funnel,legal,wsa,ui}/. The package is kept
// as a structural placeholder so the workspace mirrors awfx's
// packages/ui entry — it can be re-populated when a true cross-app
// primitive is needed.
export {};
